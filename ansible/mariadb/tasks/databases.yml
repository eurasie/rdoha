# Tasks to create databases


- name: Create {{ mariadb_app_database }} database
  mysql_db:
    login_user: root
    login_password: "{{ mariadb_root_password }}"
    login_unix_socket: "{{ mariadb_home }}/mysql.sock"
    name: "{{ mariadb_app_database }}"
    state: present